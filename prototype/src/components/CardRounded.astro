<style>
  :root {
  font-family: Inter, system-ui, Avenir, Helvetica, Arial, sans-serif;
  line-height: 1.5;
  font-weight: 400;
  color-scheme: light;
  background-color: #f5f5f5;
}

body {
  margin: 0;
  display: flex;
  justify-content: center;
  align-items: center;
  min-height: 100vh;
  padding: 2rem;
}

#app {
  max-width: 400px;
  width: 100%;
}

.card {
  background: white;
  border-radius: 12px;
  overflow: hidden;
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1),
              0 2px 4px -1px rgba(0, 0, 0, 0.06);
  transition: transform 0.2s ease-in-out;
}

.card:hover {
  transform: translateY(-4px);
}

.card-image {
  width: 100%;
  height: 250px;
  object-fit: cover;
}

.card-content {
  padding: 1.5rem;
}

.card-meta {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 1rem;
}

.date {
  color: #6b7280;
  font-size: 0.875rem;
}

.tags {
  display: flex;
  gap: 0.5rem;
}

.tag {
  background-color: #f3f4f6;
  color: #4b5563;
  padding: 0.25rem 0.75rem;
  border-radius: 9999px;
  font-size: 0.75rem;
  font-weight: 500;
}

.card-title {
  color: #111827;
  font-size: 1.5rem;
  font-weight: 600;
  margin: 0 0 1rem 0;
}

.card-text {
  color: #4b5563;
  margin: 0 0 1.5rem 0;
  line-height: 1.625;
}

.read-more {
  color: #3b82f6;
  text-decoration: none;
  font-weight: 500;
  display: inline-flex;
  align-items: center;
  transition: color 0.15s ease-in-out;
}

.read-more:hover {
  color: #2563eb;
}
</style>

<div class="card">
  <img src="https://picsum.photos/400/250" alt="Card image" class="card-image">
  <div class="card-content">
    <div class="card-meta">
      <span class="date">March 14, 2024</span>
      <div class="tags">
        <span class="tag">Design</span>
        <span class="tag">UI</span>
      </div>
    </div>
    <h2 class="card-title">Beautiful Card Design</h2>
    <p class="card-text">
      This is a beautiful card component built with HTML and CSS. It features a clean design
      with an image, title, date, tags, and description.
    </p>
    <a href="#" class="read-more">Read More →</a>
  </div>
</div>
